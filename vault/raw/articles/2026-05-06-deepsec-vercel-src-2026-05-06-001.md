# Introducing deepsec: The security harness for finding vulnerabilities in your codebase

Captured from a user-supplied Vercel blog page transcription on 2026-05-06.

Canonical URL:
https://vercel.com/blog/introducing-deepsec-find-and-fix-vulnerabilities-in-your-code-base

---

Source positioning captured from the supplied text:

- Vercel blog post
- Author: Malte Ubl
- Published: May 4, 2026
- Announces `deepsec` as an open-source security harness powered by coding agents.

Core product framing captured from the supplied text:

- `deepsec` is described as running on infrastructure controlled by the user.
- It can run on a laptop without giving privileged source access to a cloud service.
- For inference, it is described as working with existing Claude or Codex subscriptions.
- Large scans can take days on one machine, with optional fanout to Vercel Sandboxes for parallel execution.

Architecture and workflow captured from the supplied text:

- Static scan:
  - regex-only scan to identify security-sensitive files
- Investigate:
  - coding agents investigate each candidate file
- Revalidate:
  - a second agent run validates findings and reduces false positives
- Enrich:
  - findings are mapped to responsible contributors using git metadata and optional services
- Export:
  - findings are formatted into actionable instructions or tickets

Model and reasoning notes captured from the supplied text:

- The article says deepsec uses Claude and Codex for tailored investigation.
- Specifically named:
  - Opus 4.7 at max effort
  - GPT 5.5 at xhigh reasoning
- The article also discusses optional use of “cyber” model variants, but says deepsec is functional with standard models.

Operational claims captured from the supplied text:

- Vercel says deepsec has been useful on its own monorepos and customer codebases.
- It includes testimonials claiming good true-positive rates and actionable findings.
- It cites a false-positive rate of roughly 10–20%.
- It says deepsec works best for applications and services rather than libraries or frameworks.

Customization notes captured from the supplied text:

- deepsec includes a plugin system.
- The most common customization described is custom scanners or regex matchers tuned to the codebase’s auth model, data layer, or team conventions.

Getting-started notes captured from the supplied text:

- `npx deepsec init` at the repo root
- creates `./.deepsec`
- stores configuration and investigation catalog there

Interpretive note:

- This source fits the catalog as agent-assisted AppSec infrastructure: not a generic SAST scanner, but a multi-stage investigation workflow that combines pattern-based candidate discovery, agent-led reasoning, revalidation, ownership mapping, and ticket-oriented export.
