# freerasp-react-native

Captured from the npm package page referenced by the user on 2026-05-17.

Canonical URL:
https://www.npmjs.com/package/freerasp-react-native

Important note:
- The user also pasted unrelated GitHub Projects documentation text.
- That pasted body does not match the npm package URL and is not treated as evidence for this source.
- This record is based on the package identity and public package-page framing associated with `freerasp-react-native`.

---

Package identity captured from the referenced page context:

- Package name: `freerasp-react-native`
- Ecosystem: `npm`
- Domain: `React Native mobile application security`

High-level framing captured from the package identity and public description context:

- `freerasp-react-native` is the React Native package for Talsec’s freeRASP mobile runtime-application self-protection tooling.
- The package is positioned around in-app runtime threat detection rather than general analytics or ordinary app utility behavior.

Security and runtime-protection themes associated with the package context:

- runtime application self-protection (`RASP`)
- mobile threat detection
- Android and iOS runtime-integrity checks
- React Native integration layer for native security checks

Interpretive note:

- This source is currently captured conservatively because the pasted body did not correspond to the URL, and the available package-page context in this session was limited. The durable value is simply that `freerasp-react-native` belongs in the catalog as a React Native mobile-security package centered on runtime threat detection and self-protection.
