# Paneru

Captured from a user-supplied GitHub repository page transcription on 2026-05-11.

Canonical URL:
https://github.com/karinushka/paneru

---

Repository positioning captured from the supplied page:

- `karinushka/paneru`
- Public repository
- Described as a sliding, tiling window manager for macOS.
- Inspired by Niri-like scrollable tiling behavior and PaperWM-style workflows.

Public repository metadata captured from the supplied page:

- Stars shown: 1.4k
- Forks shown: 59
- Latest release shown: `v0.4.1`

Core UX and feature claims captured from the supplied page:

- Windows are arranged on an infinite horizontal strip extending to the right.
- Opening a new window should not resize existing windows.
- Each monitor has its own independent strip.
- Works with native macOS workspaces and also exposes experimental virtual workspaces.
- Focus-follows-mouse support.
- Touchpad-based navigation of the window strip.
- Native macOS tabs support.

Operational and integration notes captured from the supplied page:

- Requires accessibility permissions.
- Recommends “Displays have separate spaces.”
- Documents workarounds for off-screen window behavior and multi-display arrangements.
- Supports installation through Cargo, GitHub source, Homebrew, and Nix.
- Can run in the foreground or as a service.

Command and automation notes captured from the supplied page:

- Exposes `send-cmd` over a Unix socket at `/tmp/paneru.socket`.
- Commands include focus, swap, resize, centering, stacking, display movement, virtual workspace movement, pointer warping, and quit.
- The README explicitly suggests shell-script and automation use cases around `send-cmd`.

Architecture notes captured from the supplied page:

- Uses Bevy ECS as the architectural core.
- Splits the system into:
  - platform layer
  - management layer
  - ECS layer
- The repository also points to `ARCHITECTURE.md` and `CONFIGURATION.md` for deeper documentation.

Interpretive note:

- This source fits the catalog as both end-user productivity tooling and a reference architecture for a Rust-based macOS window manager built on ECS concepts.
