# NativeScript

Captured from the user-supplied URLs on 2026-05-28, with the public pages checked directly.

Canonical URL:
https://nativescript.org/

Companion snippets URL:
https://snippets.nativescript.org/

Source type:
- official project website
- official or project-adjacent snippets/examples site

Core project framing captured from the official site:

- NativeScript is positioned as a framework for building native mobile apps with JavaScript or TypeScript.
- The official site frames NativeScript around native platform UI rather than WebView rendering.
- It says NativeScript uses a shared set of components that map to each platform's native UI building blocks.
- It also highlights direct access to native APIs from TypeScript.

Framework integration framing captured from the official site and documentation search results:

- NativeScript can be used standalone.
- It also supports common frontend frameworks and integrations, including:
  - Angular
  - React
  - Solid
  - Svelte
  - Vue

Documentation framing captured from the official docs search result:

- NativeScript provides platform APIs directly to the JavaScript runtime.
- The documentation emphasizes strong TypeScript support for platform API access.

Snippets site framing captured from the supplied snippets URL:

- The snippets site is a practical examples surface for NativeScript code.
- The visible snippets include tasks such as:
  - reading app ID and version details from Android package metadata
  - bridging JavaScript or TypeScript code to Android Kotlin code for Toast behavior
- This makes the snippets site useful as a cookbook-style reference for direct native API access and small platform integrations.

Interpretive note:

- This source is strongest as a cross-platform native mobile framework reference. Its durable value is the contrast with WebView-based stacks: NativeScript lets JavaScript or TypeScript code target native UI and native platform APIs directly, while the snippets site shows the concrete utility layer where that native access becomes day-to-day app code.
