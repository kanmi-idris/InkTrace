# Orval documentation

Captured from the user-supplied documentation URL on 2026-05-26.

Canonical URL:
https://orval.dev/docs

Source type:
- official project documentation

Project framing captured from the docs entry point and surrounding context:

- Orval is positioned as tooling that generates API clients and related frontend integration code from OpenAPI specifications.
- The documentation is the primary reference surface for configuration, generators, output modes, and framework integration patterns.

Core capability framing inferred from the docs surface and linked usage pattern:

- OpenAPI-driven code generation
- typed client generation
- frontend integration support around request layers and query layers
- configurable output structure
- repeatable regeneration when backend schemas change

Most relevant durable use case in this vault’s current context:

- Orval is especially useful as a mechanical data-layer generator for applications using TanStack Query or similar client-side data-fetching stacks.
- Instead of hand-writing endpoint functions, request and response types, and repetitive query wrappers, teams can derive these artifacts from the API schema and keep them synchronized as the backend evolves.

Interpretive note:

- This source is strongest as API-schema-to-frontend-tooling infrastructure. Its durable value is the shift from hand-maintained CRUD wrappers toward generated, schema-aligned clients and query layers that reduce drift between backend contracts and frontend data access code.
