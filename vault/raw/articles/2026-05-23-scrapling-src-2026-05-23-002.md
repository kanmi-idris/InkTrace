# Scrapling

Captured from the public GitHub repository referenced by the user on 2026-05-23.

Canonical URL:
https://github.com/D4Vinci/Scrapling

---

Repository framing captured from the public README:

- Scrapling is described as:
  - “an adaptive Web Scraping framework that handles everything from a single request to a full-scale crawl”
  - “Effortless Web Scraping for the Modern Web”
- The README frames the project as one library spanning:
  - selection methods
  - fetchers
  - spiders
  - proxy rotation
  - CLI
  - MCP

Core capability framing captured from the README:

- The parser is said to learn from website changes and automatically relocate elements after structure updates.
- Fetchers are positioned around:
  - standard HTTP requests
  - stealthy anti-bot-aware fetching
  - dynamic browser-backed fetching
- The spider framework is described as scaling from simple requests to concurrent multi-session crawls.

Key features captured from the public README:

- Scrapy-like spider API
- concurrent crawling
- per-domain throttling
- multi-session support
- pause and resume via checkpoints
- streaming crawl mode
- blocked-request detection and retries
- optional `robots.txt` compliance
- development mode with cached responses
- built-in export helpers
- proxy rotation
- domain and ad blocking
- DNS-over-HTTPS support
- async support

Adaptive scraping and AI-related features captured from the public README:

- smart element tracking after site changes
- smart flexible selection using:
  - CSS selectors
  - XPath
  - filter-based search
  - text search
  - regex search
- similar-element finding
- built-in MCP server for AI-assisted scraping and extraction

Developer-experience notes captured from the public README:

- interactive scraping shell
- CLI extraction without writing code
- auto-selector generation
- familiar API inspired by Scrapy, BeautifulSoup, and Parsel
- full type hints
- Docker image with browsers included

Performance and ecosystem signals captured from the public README:

- Benchmarks are published in the README comparing parser speed to other Python scraping libraries.
- The public repo page in this session showed roughly:
  - 47.2k stars
  - 4.4k forks
  - latest visible release `v0.4.7` dated `Apr 17, 2026`
- The README states Python 3.10+ is required.
- The license is BSD-3-Clause.

Risk and ethics note captured from the public README:

- The project includes an explicit educational-and-research disclaimer and tells users to comply with local laws, site terms of service, and `robots.txt`.

Interpretive note:

- This source is strongest as scraping-infrastructure and agent-tooling material. Its durable value is that it combines adaptive parsing, stealth or anti-bot-aware fetching, large-scale crawl orchestration, and MCP-based AI integration into one Python framework, rather than focusing on only one layer of the scraping stack.
