# Baguette

Captured from user-supplied GitHub repository page transcriptions on 2026-05-02 and 2026-05-16.

Canonical URL:
https://github.com/tddworks/baguette

---

Repository positioning captured from the supplied page:

- `tddworks/baguette`
- Public repository
- Described as a headless iOS Simulator manager with host-side input injection for iOS 26.
- Framed as a single Swift CLI that can create, boot, and shut down simulator devices, stream their screens, and inject gestures without launching the Simulator.app GUI.

Public repository metadata captured from the supplied page:

- Earlier capture:
  - Stars shown: 145
  - Forks shown: 7
  - Repository age shown: 1 day old
  - Latest release shown: `baguette v0.1.3`
- Updated capture on 2026-05-16:
  - Stars shown: 954
  - Forks shown: 43
  - Repository age shown: 2 weeks old
  - Latest release shown: `baguette v0.1.73`

Core capabilities captured from the supplied page:

- Headless simulator management.
- 60 fps screen streaming.
- Host-HID input injection including taps, swipes, one- and two-finger gestures, home, lock, and scroll.
- Optional self-contained web UI served on localhost.
- A multi-device “device farm” dashboard at `/farm`.

Expanded capabilities captured from the newer supplied page:

- The CLI and browser UI now also advertise:
  - keyboard input
  - broader hardware-button coverage
  - app-switcher and system edge gestures
  - device-orientation control
  - screenshots and recordings
  - accessibility-tree inspection and hit testing
  - live unified-log streaming
  - a virtual-camera pipeline that feeds a Mac webcam into simulator camera APIs

Streaming notes captured from the supplied page:

- Frame streaming is described as supporting MJPEG or H.264 / AVCC over stdout or WebSocket.
- Runtime-adjustable bitrate, fps, and scale are documented.
- `baguette stream` outputs binary frames and accepts JSON-based runtime control commands via stdin.

Web UI and device-farm notes captured from the supplied page:

- `baguette serve` exposes browser control surfaces at `/simulators` and `/farm`.
- `/simulators` is described as a list and per-device stream interface.
- `/farm` is described as a multi-device dashboard with grid, wall, and list views, filtering, sorting, and a focus pane.
- The focused tile can receive gestures and hardware-button input through the same pipeline as the CLI.

Newer UI notes captured from the updated page:

- The per-device interface is now framed as a focus-mode page with:
  - camera controls
  - accessibility inspector
  - logs panel
  - screenshot and app-switcher controls
  - in-browser MP4 recording
- The project also exposes a small browser SDK under `Resources/Web/baguette/` for mounting pieces of the simulator UI from JavaScript.

Accessibility, logs, and camera notes captured from the updated page:

- `baguette describe-ui` is described as returning the on-screen accessibility tree as JSON, including hit testing by coordinates.
- `baguette logs` is described as streaming simulator unified logs with filter options.
- The camera feature is described as new in `0.1.72`, using a vendored `VirtualCamera.dylib` loaded into simulator apps through `DYLD_INSERT_LIBRARIES` and a shared-memory ring buffer fed from a Mac webcam.

Architecture and implementation notes captured from the supplied page:

- Built in Swift with a layered Domain / Infrastructure / App split.
- Uses Hummingbird and WebSocket support on the server side.
- Uses private SimulatorKit and CoreSimulator frameworks from Xcode 26.
- The earlier page claimed 110+ tests; the updated page claims 460+ tests and a much broader set of mockable boundaries spanning input, screen, accessibility, logs, orientation, camera capture, simulator injection, and related ports.

Low-level implementation notes captured from the updated page:

- The newer README is much more explicit about why the tool works on iOS 26.4:
  - updated 9-argument Indigo HID message usage
  - IOHID event construction for streaming touches and system edge gestures
  - a per-app virtual-camera dylib injection strategy for camera substitution
- The source layout now includes separate `VirtualCamera/`, `Accessibility/`, `Logs/`, `Orientation/`, and richer web-SDK subtrees.

Interpretive note:

- This source fits the catalog as simulator automation and device-farm infrastructure for iOS workflows, but the newer README makes it clear that the project has expanded beyond “headless simulator control” into a fuller remote-debugging and test-harness surface: camera mocking, accessibility inspection, log streaming, orientation control, and a browser SDK on top of the same simulator transport.
