# Expo vector-icons deprecation tweet

Captured from public X oEmbed and page metadata on 2026-06-10.

Primary URL:
https://x.com/expo/status/2064342664317726866?s=20

Author captured:
- Expo

Publicly visible text captured from the post:

- "expo/vector-icons is moving to deprecated."
- "The react-native-vector-icons packages now integrate directly with expo-font and work in Expo Go, dev builds...the whole thing."
- "The change is going to require a bit of effort. But it'll..."

Evidence limitations:

- The public oEmbed and page metadata expose only a truncated version of the final sentence.
- No public capture in this ingest exposed the full migration instructions, exact replacement import path guidance, or the linked follow-up material if any.

Interpretive note:

- This is strong evidence of an Expo ecosystem migration: `expo/vector-icons` is being deprecated in favor of direct `react-native-vector-icons` usage integrated with `expo-font`.
- It is also strong evidence that Expo now expects the upstream icon packages to work in Expo Go and dev builds, which reduces the historical need for the Expo wrapper.
- The operational details of the migration should remain conservative until stronger documentation or a full thread is captured.