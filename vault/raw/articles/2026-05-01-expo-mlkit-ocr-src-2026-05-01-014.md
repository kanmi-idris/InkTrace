# expo-mlkit-ocr

Captured from a user-supplied npm package page transcription on 2026-05-01.

Canonical URL:
https://www.npmjs.com/package/expo-mlkit-ocr

---

Package positioning captured from the supplied page:

- `expo-mlkit-ocr`
- Version shown: `0.2.6`
- Public package
- Published 5 hours ago at capture time
- Described as a production-ready Expo Module for on-device text recognition using Google ML Kit Text Recognition v2 for both iOS and Android.

Feature claims captured from the supplied page:

- On-device OCR with no network requests required.
- Google ML Kit Text Recognition v2 rather than Firebase ML Kit.
- Structured output with blocks, lines, and elements plus bounding boxes.
- Native iOS and Android implementations using Expo Modules API.
- TypeScript support.
- Expo config plugin for setup.
- `OCRTextOverlay` component for visualizing and selecting recognized text.

Setup and compatibility notes captured from the supplied page:

- Install command:
  - `npx expo install expo-mlkit-ocr expo-image-picker expo-build-properties`
- `expo-build-properties` is described as required to set iOS deployment target `16.0`.
- The config examples show use of both `expo-mlkit-ocr` and `expo-build-properties` plugins.
- The page explicitly says the module will not work in Expo Go and requires a dev client or EAS Build.
- Local prebuild and EAS build workflows are both documented.

API and output notes captured from the supplied page:

- Primary API:
  - `recognizeText(uri: string): Promise<RecognitionResult>`
- Output shape includes:
  - top-level `text`
  - `blocks`
  - each block includes `lines`
  - each line includes `elements`
  - all levels include bounding boxes
- Bounding boxes are described as using the image’s native coordinate system.

Overlay component notes captured from the supplied page:

- `OCRTextOverlay` wraps an image and renders interactive bounding boxes.
- It supports block, line, or element highlight levels.
- It supports multi-select and single-select interaction.
- It exposes styling controls for box color, selected color, opacity, stroke width, and corner radius.
- It is described as pure React Native with no canvas dependency.

iOS engine and simulator notes captured from the supplied page:

- The package supports `iosEngine` values:
  - `auto`
  - `mlkit`
  - `vision`
- Apple Vision is presented as a fallback for simulator compatibility when ML Kit binaries do not support the relevant simulator runtime.

Interpretive note:

- This source fits the React Native and Expo notes as a native OCR utility with structured text extraction and an unusually product-ready overlay primitive for selection and review workflows.
