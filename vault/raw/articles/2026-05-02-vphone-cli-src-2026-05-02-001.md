# vphone-cli

Captured from a user-supplied GitHub repository page transcription on 2026-05-02.

Canonical URL:
https://github.com/Lakr233/vphone-cli

---

Repository positioning captured from the supplied page:

- `Lakr233/vphone-cli`
- Public repository
- Stars shown: 5.9k
- Forks shown: 892
- Described as a way to boot a virtual iPhone via Apple’s `Virtualization.framework` using PCC research VM infrastructure.

High-level scope captured from the supplied page:

- The project centers on booting virtualized iPhone environments on macOS 15+ hosts.
- It documents multiple firmware patch variants with increasing levels of modification and bypass.
- It includes host-side workflows for VM creation, firmware preparation, restore, custom-firmware install, boot, backup, and switching between VM states.

Testing and automation notes captured from the supplied page:

- The repository says `vphone-cli` exposes a host control socket for programmatic VM interaction.
- Listed actions include screenshots, touch injection, swipe gestures, hardware keys, and clipboard operations.
- Each action is described as returning a compact grayscale screenshot inline.
- The page explicitly frames this as enabling AI-driven end-to-end testing workflows.
- It also references `vphone-mcp` as an MCP server wrapper with higher-level app-control tools.

Operational notes captured from the supplied page:

- The README describes prerequisites around macOS version, Apple virtualization support, and host configuration changes.
- It includes quick-start and manual setup paths, VM configuration through `config.plist`, restore flows, ramdisk flows, and post-boot connectivity such as SSH, VNC, and RPC.
- It also documents multi-VM backup and switching workflows.

Interpretive note:

- This source is relevant as device-virtualization and automation infrastructure, especially for people experimenting with AI-controlled mobile testing or deeper iOS research environments.
- It also clearly overlaps with firmware-modification and jailbreak research, so downstream synthesis should distinguish benign testing uses from more sensitive bypass-oriented material.
